# PUBLIC_INTERFACE
# Example environment variables for tv_frontend dev server
# Purpose: Configure Vite dev server host and port via env (do NOT pass CLI flags after npm run dev)

# Port to bind the Vite dev server. Defaults to 3000 if unset.
PORT=3000

# Hostname allowed by dev server. Use 0.0.0.0 for all interfaces in containers.
# Note: Binding is enforced to 0.0.0.0 by the launcher/vite config; this value is used for allowedHosts.
HOST=0.0.0.0
