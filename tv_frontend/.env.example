# PUBLIC_INTERFACE
# Example environment configuration for tv_frontend dev server.
# Purpose: set host/port via environment, not via CLI flags.
# Usage:
#   cp .env.example .env
#   # Optionally edit PORT and HOST
#   PORT=3000
#   HOST=0.0.0.0

# Port for Vite dev server. Must be numeric. Default is 3000 if unset/invalid.
PORT=3000

# Hostname to allow in dev server (allowedHosts). Common values:
# - 0.0.0.0 (bind-all)
# - localhost
# - 127.0.0.1
HOST=0.0.0.0
