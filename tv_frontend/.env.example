# PUBLIC_INTERFACE
# tv_frontend environment variables used by the stable dev launcher (bin/start-dev.js)
# and vite.config.js. Copy this file to .env and adjust as needed.
#
# Important: Do NOT pass CLI flags after "npm run dev". Use these env variables instead.

# Host to bind and to allow in Vite allowedHosts list. Typical values:
# - 0.0.0.0 for container binding
# - localhost for local development
HOST=0.0.0.0

# Port for dev server. Keep at 3000 to align with CI expectations.
PORT=3000

# When set, enables chokidar polling mode (useful in some containerized FS)
# VITE_WATCH_POLLING=false
