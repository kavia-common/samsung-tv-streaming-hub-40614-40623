# PUBLIC_INTERFACE
# tv_frontend environment variables
# Purpose: Configure Vite dev server via environment variables, not CLI flags.
# Do NOT use: npm run dev -- --port 3000 --host 0.0.0.0
# Instead set:
#   PORT=3000 HOST=0.0.0.0 npm run dev
#
# PORT: Port for Vite dev server (strictPort enforced). Default: 3000.
PORT=3000

# HOST: Optional hostname to allow via allowedHosts (e.g., CI hostname).
# Examples:
#   HOST=0.0.0.0
#   HOST=localhost
#   HOST=my-ci-host.internal
HOST=0.0.0.0

# VITE_WATCH_POLLING: Set to "true" to force file watcher polling (rarely needed).
# VITE_WATCH_POLLING=false
