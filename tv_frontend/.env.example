# Example environment variables for tv_frontend dev server
# Use these instead of passing CLI flags to npm scripts.
# The dev launcher reads these once at startup.

# PUBLIC_INTERFACE
# PORT: Dev server port. Defaults to 3000 if unset.
PORT=3000

# HOST: Optional host to add to Vite allowedHosts (binding remains 0.0.0.0)
# e.g., HOST=my-ci-host.internal
# HOST=
