# PUBLIC_INTERFACE
# Example environment variables for tv_frontend dev server
# Use these instead of passing CLI flags after `npm run dev`
# PORT must be numeric; HOST is optional and used for allowedHosts in vite.config.js
PORT=3000
HOST=0.0.0.0

# Optional: enable polling in environments with limited fs events
# VITE_WATCH_POLLING=true
